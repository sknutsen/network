apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingressroute.yaml
# App Deployment/Service come from Flux GitRepository → github.com/sknutsen/Zdk
# when that repo ships manifests. This IngressRoute 502s until then.
