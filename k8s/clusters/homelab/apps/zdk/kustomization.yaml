apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingressroute.yaml
  - gitrepository.yaml
# App Deployment/Service come from Flux GitRepository → github.com/sknutsen/Zdk
# (suspended in gitrepository.yaml until that repo ships manifests).
