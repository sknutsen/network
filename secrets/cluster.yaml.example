# Copy to cluster.yaml and encrypt with sops once age keys exist.
# Flux / k8s secrets for the homelab cluster (Stage 5+). Do not commit the
# live cluster.yaml.
#
# sops --encrypt --in-place secrets/cluster.yaml
#
# Example keys — replace when a controller actually consumes them:
# grafana:
#   adminPassword: REPLACE
# longhorn:
#   backupTarget: REPLACE
