# Copy to .env (gitignored). DB_PASSWORD and POSTGRES_PASSWORD must match.
# Use only A-Za-z0-9 (Immich postgres requirement).
DB_PASSWORD=changeme
POSTGRES_PASSWORD=changeme
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
