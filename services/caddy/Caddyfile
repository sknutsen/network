# Edge proxy on TrueNAS (10.10.30.20)
# WAN port-forward: 443/80 only. ACME certs stored in tank/services/caddy/data

# Public services (Stage 7)
# zdk.no — enable when github.com/sknutsen/Zdk ships a deployable container
zdk.no {
	reverse_proxy 10.10.30.100:80
	encode gzip
}

code.zdk.no {
	reverse_proxy forgejo:3000
	encode gzip
}

# Internal homelab services (*.lab.zdk.no) — VPN/trusted VLAN; Authelia forward_auth
# Example (uncomment and extend when deploying):
#
# grafana.lab.zdk.no {
#     forward_auth http://authelia:9091 {
#         uri /api/authz/forward-auth
#     }
#     reverse_proxy 10.10.30.100:80
# }
