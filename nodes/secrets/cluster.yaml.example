# Copy to cluster.yaml and encrypt with sops (nodes/.sops.yaml).
# Do not commit plaintext. The live file is encrypted in git.
#
#   sops --encrypt --in-place nodes/secrets/cluster.yaml
#
# Token: nordri `sudo cat /var/lib/rancher/k3s/server/node-token`

k3s:
  token: REPLACE
